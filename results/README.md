# Results

This folder is reserved for curated result summaries. Uncurated generated outputs should remain under `failure_maps_outputs/`, which is ignored by Git.

Use `data/source_data/` for compact manuscript-facing source-data tables when redistribution is permitted.
